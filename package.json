{
  "name": "ardoise",
  "version": "1.1.0",
  "private": true,
  "description": "Ardoise — analyse des prelevements des plateformes de livraison",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\""
  },
  "dependencies": {
    "stripe": "^17.0.0"
  }
}
